<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Compass.Runtime</RootNamespace>
    <AssemblyName>Compass.Runtime</AssemblyName>
    <Description>The Compass value model: fractions, sets, optionals, deep equality, and the built-in exceptions. Every compiled Compass program references this.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Compass.Tests" />
  </ItemGroup>

</Project>
